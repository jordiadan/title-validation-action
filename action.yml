name: 'Validate Pull Request Title'
description: 'Validates that the pull request title starts with FEATURE, FIX, TECH, or DOCS'
author: 'Your Name'
inputs:
  github_token:
    description: 'The GitHub token for making API calls'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'